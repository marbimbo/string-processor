spring:
  main:
    banner-mode: "OFF"
  datasource:
    url: jdbc:h2:file:~/demo
    username: sa
    password:
    driverClassName: org.h2.Driver
    initialization-mode: always # insert test data
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create

filename: "input.txt.gz"

logging:
  level:
    root: INFO